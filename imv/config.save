[options]
# Suppress the overlay text completely
overlay = false

# Set a clean window title (just the file name)
[options]
# Suppress the overlay text completely
overlay = false

# Set a clean window title (just the file name)
title_text = imv - [text [name]]
